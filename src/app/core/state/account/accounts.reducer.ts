import { AccountActions, AccountActionTypes } from './accounts.actions';
import { featureAdapter, State } from "./accounts.state";

const initialState = featureAdapter.getInitialState();

export function accountsReducer(
	state = initialState,
	action: AccountActions
): State {

	switch(action.type) {

		case AccountActionTypes.COLLECTION_LOADED_SUCCESS:

			return featureAdapter.setAll(
				action.payload.collection,
				{...state,
					//collection: [],
					//selectedItem: null,
					isLoading: false,
					error: null,
					collectionLoaded:true
				}
			);

		case AccountActionTypes.COLLECTION_CLEAR_ALL:

			return featureAdapter.removeAll(state);

		case AccountActionTypes.MODEL_LoadedSuccess:

			//console.warn('action.type:', action.type);

			return featureAdapter.addOne(
				action.payload.dataItem,
				{
					...state,
					//collection: [],
					//selectedItem: null,
					isLoading: false,
					collectionLoaded:true,
					error: null
				  }
			);

		case AccountActionTypes.MODEL_CreateSuccess:

			return featureAdapter.addOne(
				action.payload.dataItem,  {
					...state,
					collectionLoaded:true,
					isLoading: false,
					error: null
				  }
			);

		case AccountActionTypes.MODEL_UpdateSuccess:

			return featureAdapter.updateOne(
				action.payload.dataItem,
				{
					...state,
					collectionLoaded:true,
					isLoading: false,
					error: null
				}
			);

		case AccountActionTypes.MODEL_DeleteSuccess:

			return featureAdapter.removeOne(
				action.payload.dataId,
				{
					...state,
					allPositionsLoaded: true,
					isLoading: false,
					error: null
				}
			);

		case AccountActionTypes.MODEL_LoadedFailed:
		case AccountActionTypes.MODEL_UpdateFailed:
		case AccountActionTypes.MODEL_UpdateStatusFailed:
		case AccountActionTypes.MODEL_DeleteFailed:
		case AccountActionTypes.MODEL_RestoreFailed:
		case AccountActionTypes.MODEL_CreateFailed:
		case AccountActionTypes.COLLECTION_LOADED_FAILED:

			console.error('FAILED REDUCER:', action.type, featureAdapter);

			return state;

		default: {

			//console.warn('DEFAULT REDUCER:', action.type, featureAdapter);

			return state;

			return {
				...state,
				//collection: [],
				//selectedItem: null,
				collectionLoaded:true,
				isLoading: false,
				error: null
			};
		}

	}
}

export const {
selectAll,
selectEntities,
selectIds,
selectTotal

} = featureAdapter.getSelectors();
